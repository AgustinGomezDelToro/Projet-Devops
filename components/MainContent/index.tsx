import { Box } from "@chakra-ui/react";

function MainContent() {
    return (
        <Box flex="1" p={4}>
            {/* Aquí se mostrarán los contenidos de agendas, historia clínica, etc. */}
            <p>Contenido principal del dashboard</p>
        </Box>
    );
}

export default MainContent;
