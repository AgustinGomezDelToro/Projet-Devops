import Login from '../components/auth/login';

function LoginPage() {
    return (
        <div>
            <h1>Iniciar Sesión</h1>
            <Login />
        </div>
    );
}

export default LoginPage;
